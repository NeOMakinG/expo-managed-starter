import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
  safeArea: {
    width: '100%', 
    alignItems: 'center' 
  },
  title: {
    fontSize: 20,
    fontWeight: 'bold',
  },
  inputLabel: {
    marginBottom: 5
  }
});

export default styles;
